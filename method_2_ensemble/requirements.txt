# AI Image Detector — New Approach
# Uses Dockerfile to control exact versions (bypasses HF forced Gradio 6)

# PyTorch CPU
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu

# Core — pinned Gradio 5.12.0 (stable, tested)
gradio==5.12.0
transformers>=4.38.0
Pillow>=9.0.0
numpy<2.0
matplotlib>=3.7.0
fastapi
uvicorn[standard]
python-multipart
requests
